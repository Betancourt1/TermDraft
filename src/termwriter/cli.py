"""Command-line entry point for TermWriter."""

from collections.abc import Sequence


def main(argv: Sequence[str] | None = None) -> int:
    """Launch TermWriter."""
    del argv
    raise RuntimeError("TermWriter's application scaffold is not complete yet")
